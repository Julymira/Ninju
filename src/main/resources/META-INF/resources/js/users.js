requireAdmin(); // só ADMIN acessa esta página

// TODO: carregar lista de usuários — apiGet('/users')
// TODO: formulário para criar usuário — apiPost('/users', { name, email, password, role })
// TODO: botão editar — apiPut('/users/{id}', dados)
// TODO: botão excluir — apiDelete('/users/{id}')
