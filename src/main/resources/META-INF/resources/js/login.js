// TODO: capturar submit do formulário de login
// Chamar: apiPost('/auth/login', { email, password })
// Em caso de sucesso: saveSession(token, name, role) e redirecionar para /pages/dashboard.html
// Em caso de erro: exibir mensagem para o usuário
